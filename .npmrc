# react-simple-maps@3 aún declara peers de React 16/17/18 y no de React 19,
# aunque funciona bien con 19. Sin esto, `npm install` falla con ERESOLVE
# (localmente y en el build de Vercel, que corre npm install sin flags).
legacy-peer-deps=true
